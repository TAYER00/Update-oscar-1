import 'package:flutter/material.dart';

// class LocationFormModel extends ChangeNotifier {
//   String _name = '';  // Nom de l'utilisateur
//   String _phone = ''; // Numéro de téléphone de l'utilisateur
//   String _localisation = ''; // URL de localisation
//   bool _isLocationAvailable = false; // Indicateur si la localisation est disponible

//   // Getter et Setter pour le 'name' (nom)
//   String get name => _name;
//   set name(String value) {
//     if (_name != value) { // Vérification de changement de valeur
//       _name = value;
//       notifyListeners();  // Notifie les écouteurs que le nom a changé
//     }
//   }

//   // Getter et Setter pour le 'phone' (numéro de téléphone)
//   String get phone => _phone;
//   set phone(String value) {
//     if (_phone != value) { // Vérification de changement de valeur
//       _phone = value;
//       notifyListeners();  // Notifie les écouteurs que le téléphone a changé
//     }
//   }

//   // Getter et Setter pour la 'localisation'
//   String get localisation => _localisation;
//   set localisation(String value) {
//     if (_localisation != value) { // Vérification de changement de valeur
//       _localisation = value;
//       notifyListeners();  // Notifie les écouteurs que la localisation a changé
//     }
//   }

//   // Getter et Setter pour 'isLocationAvailable' (si la localisation est disponible)
//   bool get isLocationAvailable => _isLocationAvailable;
//   set isLocationAvailable(bool value) {
//     if (_isLocationAvailable != value) { // Vérification de changement de valeur
//       _isLocationAvailable = value;
//       notifyListeners();  // Notifie les écouteurs que la disponibilité de la localisation a changé
//     }
//   }
// }
